# None
# Represents no value / absence of a value.
server_ip = None

if server_ip is None:
    print("IP address is not configured")
else:
    print("IP address is configure")

# Output: IP address is not configured
# Important:
# None is different from:"0, "", False"
